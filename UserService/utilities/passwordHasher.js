// const bcrypt = require('bcrypt');
// const catchAsync = require('../../utils/catchAsync');

// module.exports.hashPassword = catchAsync(async (password, salt) => {
//   const hashedPassword = await bcrypt.hash(password, salt);
//   return hashedPassword;
// });
